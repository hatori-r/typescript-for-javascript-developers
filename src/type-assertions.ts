export {};

let name:any = 'Ham';

// let length = name.length as number;
// let length = (name as string).length; // より早い段階で型アサーションの定義を行うことができる。
// let length = (<string>name).length; // 上記と同じ意味だがこの記法はJSXと似た構造になってしまうため非推奨となっている。

// length = 'foo';